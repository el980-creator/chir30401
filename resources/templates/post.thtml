<!DOCTYPE html>
<html>
<head>
    <title>Post Chirp - Chirpy</title>
    <style>
        .chirp-form {
            max-width: 500px;
            margin: 20px auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        .chirp-textarea {
            width: 100%;
            height: 100px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-family: Arial, sans-serif;
            font-size: 14px;
            resize: vertical;
        }
        .char-counter {
            text-align: right;
            margin-top: 5px;
            font-size: 12px;
            color: #666;
        }
        .char-counter.over-limit {
            color: #dc3545;
            font-weight: bold;
        }
        .submit-btn {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            margin-top: 10px;
        }
        .submit-btn:disabled {
            background-color: #6c757d;
            cursor: not-allowed;
        }
        .success-message {
            color: #28a745;
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            padding: 10px;
            border-radius: 4px;
            margin: 15px 0;
        }
        .error-message {
            color: #721c24;
            background-color: #f8d7da;
            border: 1px solid #f5c6cb;
            padding: 10px;
            border-radius: 4px;
            margin: 15px 0;
        }
        .user-stats {
            text-align: center;
            margin: 15px 0;
            padding: 10px;
            background-color: #e9ecef;
            border-radius: 4px;
        }
        .chirp-preview {
            background-color: #ffffff;
            border: 1px solid #dee2e6;
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
        }
        .hashtag {
            color: #1da1f2;
            font-weight: bold;
        }
    </style>
    <script>
        function updateCharCount() {
            const textarea = document.getElementById('chirpText');
            const counter = document.getElementById('charCounter');
            const submitBtn = document.getElementById('submitBtn');
            const currentLength = textarea.value.length;
            const maxLength = ${maxLength};
            const remaining = maxLength - currentLength;
            
            counter.textContent = remaining + ' characters remaining';
            
            if (remaining < 0) {
                counter.className = 'char-counter over-limit';
                submitBtn.disabled = true;
            } else if (remaining < 20) {
                counter.className = 'char-counter';
                counter.style.color = '#ffc107';
                submitBtn.disabled = false;
            } else {
                counter.className = 'char-counter';
                counter.style.color = '#666';
                submitBtn.disabled = false;
            }
            
            if (currentLength === 0) {
                submitBtn.disabled = true;
            }
        }
        
        function highlightHashtags() {
            const text = document.getElementById('chirpText').value;
            const preview = document.getElementById('chirpPreview');
            
            if (text.trim() === '') {
                preview.style.display = 'none';
                return;
            }
            
            // Replace hashtags with highlighted versions
            const highlightedText = text.replace(/#(\w+)/g, '<span class="hashtag">#$1</span>');
            preview.innerHTML = '<strong>Preview:</strong><br>' + highlightedText;
            preview.style.display = 'block';
        }
        
        function handleInput() {
            updateCharCount();
            highlightHashtags();
        }
        
        window.onload = function() {
            updateCharCount();
            highlightHashtags();
        }
    </script>
</head>
<body>
    <h1>Chirpy - Post a Chirp</h1>
    
    <p>
        <a href="/">Home</a> | 
        <a href="/timeline/">Timeline</a> | 
        <a href="/post/">Post Chirp</a> | 
        <a href="/search/">Search</a> | 
        <a href="/logout/">Logout</a>
    </p>

    <div class="user-stats">
        <p><strong>Welcome, ${currentUser}!</strong></p>
        <p>You have posted ${userChirpCount} chirp<#if userChirpCount != 1>s</#if> so far.</p>
    </div>

    <#if postSuccess?? && postSuccess>
        <div class="success-message">
            <strong>Chirp posted successfully!</strong><br>
            Your chirp has been added to your timeline.
            <#if createdChirp?? && createdChirp.extractHashtags()?size gt 0>
                <br><strong>Hashtags detected:</strong> 
                <#list createdChirp.extractHashtags() as hashtag>
                    #${hashtag}<#if hashtag_has_next>, </#if>
                </#list>
            </#if>
        </div>
        <p><a href="/timeline/">View your timeline</a> | <a href="/post/">Post another chirp</a></p>
    <#else>
        <#if errorMsg??>
            <div class="error-message">
                <strong>Error:</strong> ${errorMsg}
            </div>
        </#if>

        <div class="chirp-form">
            <h3>What's happening?</h3>
            <form method="POST" action="/post/">
                <textarea 
                    id="chirpText" 
                    name="chirpText" 
                    class="chirp-textarea" 
                    placeholder="Share your thoughts... Use #hashtags to categorize your chirp!"
                    maxlength="${maxLength + 50}"
                    oninput="handleInput()"
                    required><#if chirpText??>${chirpText}</#if></textarea>
                
                <div id="charCounter" class="char-counter"></div>
                
                <div id="chirpPreview" class="chirp-preview" style="display: none;"></div>
                
                <input type="submit" id="submitBtn" value="Post Chirp" class="submit-btn">
            </form>
        </div>

        <div style="margin: 20px auto; max-width: 500px; font-size: 12px; color: #666;">
            <p><strong>Tips for great chirps:</strong></p>
            <ul>
                <li>Keep it under ${maxLength} characters</li>
                <li>Use #hashtags to join conversations</li>
                <li>Be respectful and positive</li>
                <li>Share interesting thoughts, news, or updates</li>
            </ul>
        </div>
    </#if>

    <hr>
    <p style="text-align: center; font-size: 12px; color: #666;">
        <em>Chirpy - Your favorite micro-messaging platform</em>
    </p>
</body>
</html>